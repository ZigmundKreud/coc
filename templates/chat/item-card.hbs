{{log 'item-card' this}}
<h1>{{itemName}}</h1>
{{{description}}}
{{#if limited      }} <span class="tag"> {{localize "COC.ui.limited"}} | </span> {{/if}}
{{#if spell        }} <span class="tag"> {{localize "COC.ui.spell"}} | </span> {{/if}}
{{#if ranged       }} <span class="tag"> {{localize "COC.properties.ranged"}} | </span> {{/if}} 
{{#if limitedUsage }} <span class="tag"> {{localize "COC.ui.limitedUsage"}} | </span> {{/if}}
{{#if save         }} <span class="tag"> {{localize "COC.ui.save"}} | </span> {{/if}}
{{#if activable    }} <span class="tag"> {{localize "COC.properties.activable"}} | </span> {{/if}}
{{#if heal         }} <span class="tag"> {{localize "COC.ui.heal"}} | </span> {{/if}}
{{#if attack       }} <span class="tag"> {{localize "COC.ui.attack"}} | </span> {{/if}}
{{#if buff         }} <span class="tag"> {{localize "COC.ui.buff"}} | </span> {{/if}}
{{#if useMacro     }} <span class="tag"> {{localize "COC.capacity.useMacro"}} | </span> {{/if}}